@AGENTS.md

## Package versioning

Always use exact versions (no `^` or `~`) in `package.json`. When adding or updating dependencies, pin to the exact installed version. Example: `"react": "19.2.4"`, never `"react": "^19"`.
